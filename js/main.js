// Entry Point dell'applicazione

document.addEventListener('DOMContentLoaded', () => {
    // Inizializza gli eventi della UI (Home, Bottoni, ecc.)
    UI.init();
    
    // Mostra la schermata Home
    UI.showScreen('home-screen');
});
